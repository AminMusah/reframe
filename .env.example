# Written by `npx convex dev` (it detects Next.js):
CONVEX_DEPLOYMENT=dev:adjective-animal-123
NEXT_PUBLIC_CONVEX_URL=https://adjective-animal-123.convex.cloud
# Add by hand — same deployment, `.site` origin (Better Auth handler lives there):
NEXT_PUBLIC_CONVEX_SITE_URL=https://adjective-animal-123.convex.site

# Convex-side env (set with `npx convex env set NAME value`):
#   BETTER_AUTH_SECRET  — `openssl rand -base64 32`
#   SITE_URL            — http://localhost:3000 in dev, the Vercel URL in prod
